import Button from '../components/Button'
import Card from '../components/Card'
import Container from '../components/Container'
import PageMeta from '../components/PageMeta'
import { config, whatsappUrl } from '../config'
export default function Contacto(){const groups=[['Jack Aguilar',config.socials.jack],['Neuracode',config.socials.neuracode],['Neuracode Academy',config.socials.academy]] as const;const intents=[['Proyecto / automatización','Tengo un proceso, producto o sistema donde quiero evaluar IA o automatización.','proyecto'] as const,['Formación corporativa','Quiero un workshop o programa para mi equipo.','formacion'] as const,['Academy','Quiero recibir información de próximos workshops y recursos.','academy'] as const];return <><PageMeta path="/contacto" title="Contacto | Neuracode" description="Conversa con Neuracode sobre IA, automatización, agentes, software, formación corporativa o Neuracode Academy."/><section className="page-hero"><Container><p className="eyebrow">Contacto</p><h1>Cuéntanos qué quieres mejorar.</h1><p>Cuéntanos qué proceso, producto o equipo quieres mejorar. La primera conversación sirve para aclarar contexto, no para venderte una herramienta.</p><div className="actions"><Button href={whatsappUrl('proyecto')} target="_blank" rel="noreferrer">Proyecto o consultoría</Button><Button href={whatsappUrl('formacion')} target="_blank" rel="noreferrer" variant="secondary">Formación corporativa</Button><Button href={`mailto:${config.brand.email}`} variant="ghost">{config.brand.email}</Button></div></Container></section><section className="section section-tint"><Container><div className="grid grid-3">{intents.map(([title,description,intent])=><Card key={title}><p className="evidence-label">Contacto</p><h2>{title}</h2><p>{description}</p><Button href={whatsappUrl(intent)} target="_blank" rel="noreferrer" variant="secondary">{title==='Academy'?'Seguir Academy':'Escribir por WhatsApp'}</Button></Card>)}</div></Container></section><section className="section"><Container><div className="grid grid-3">{groups.map(([name,links])=><Card key={name}><h2>{name}</h2>{links.map(s=><a className="contact-link" key={s.label} href={s.url} target="_blank" rel="noreferrer">{s.label}<span>↗</span></a>)}</Card>)}</div></Container></section></>}
