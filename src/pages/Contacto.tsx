import Button from '../components/Button'; import Card from '../components/Card'; import Container from '../components/Container'; import PageMeta from '../components/PageMeta'; import { config, whatsappUrl } from '../config'
export default function Contacto(){const groups=[['Jack Aguilar',config.socials.jack],['Neuracode',config.socials.neuracode],['Neuracode Academy',config.socials.academy]] as const; return <><PageMeta path="/contacto" title="Contacto — Neuracode" description="Conversa con Neuracode sobre proyectos, formación corporativa, Academy o alianzas."/><section className="page-hero"><Container><p className="eyebrow">Contacto</p><h1>Empecemos por entender la necesidad.</h1><p>Cuéntanos qué proceso, producto o equipo quieres mejorar. La primera conversación sirve para aclarar contexto, no para venderte una herramienta.</p><div className="actions"><Button href={whatsappUrl('proyecto')} target="_blank" rel="noreferrer">Proyecto o consultoría</Button><Button href={whatsappUrl('formacion')} target="_blank" rel="noreferrer" variant="secondary">Formación corporativa</Button><Button href={`mailto:${config.brand.email}`} variant="ghost">{config.brand.email}</Button></div></Container></section><section className="section"><Container><div className="grid grid-3">{groups.map(([name,links])=><Card key={name}><h2>{name}</h2>{links.map(s=><a className="contact-link" key={s.label} href={s.url} target="_blank" rel="noreferrer">{s.label}<span>↗</span></a>)}</Card>)}</div></Container></section></>}
