import Card from '../components/Card'
import Container from '../components/Container'
import PageMeta from '../components/PageMeta'
import SectionCTA from '../components/SectionCTA'
import { config, whatsappUrl } from '../config'
import { resources } from '../content'
const topics = ['AI Automation', 'Agentic AI', 'AI & SDLC', 'Software Architecture', 'Data & Predictive AI', 'Security / Governance', 'Business / ROI']
export default function Recursos(){return <><PageMeta path="/recursos" title="Recursos de IA, Agentes y Software | Neuracode" description="Artículos y proyectos sobre automatización, agentes de IA, software, arquitectura, Secure SDLC, datos y adopción empresarial."/><section className="page-hero"><Container><p className="eyebrow">Recursos</p><h1>Ideas y recursos para aplicar IA con ingeniería.</h1><p>Artículos y proyectos públicos sobre automatización, sistemas agénticos, software, datos, seguridad y adopción empresarial.</p></Container></section><section className="section"><Container><div className="resource-topics"><p className="eyebrow">Temas que cubrimos</p><div className="topic-chips" aria-label="Temas que cubrimos">{topics.map(topic=><span key={topic}>{topic}</span>)}</div></div><div className="grid grid-3 resource-grid motion-grid">{resources.map(resource=><Card key={resource.id}><p className="evidence-label">{resource.type}</p><h2>{resource.title}</h2><p>{resource.summary}</p><p className="resource-author">Por {resource.author}</p><div className="chip-row">{resource.topics.map(topic=><span key={topic}>{topic}</span>)}</div><a className="text-link" href={resource.url} target="_blank" rel="noreferrer">Ver recurso ↗</a></Card>)}</div><SectionCTA title="¿Quieres llevar una idea a tu propio contexto?" description="Agendemos una conversación para revisar el proceso, los datos y el resultado que buscas."/></Container></section><section className="section section-tint"><Container><p className="eyebrow">Seguir a Jack</p><h2>Ideas y trabajo técnico en canales públicos.</h2><div className="social-row">{config.socials.jack.filter(({label})=>['LinkedIn','Instagram','TikTok'].includes(label)).map(s=><a key={s.label} href={s.url} target="_blank" rel="me noreferrer">{s.label} ↗</a>)}</div><SectionCTA title="¿Prefieres conversar sobre un caso concreto?" description="Cuéntanos qué estás evaluando y ordenamos el siguiente paso."/></Container></section><section className="cta"><Container><p className="eyebrow">Llevémoslo a tu contexto</p><h2>¿Tienes un proceso o sistema que estás evaluando mejorar con IA?</h2><p>Podemos revisar el problema, los datos y las restricciones antes de decidir qué tecnología usar.</p><a className="button button--primary" href={whatsappUrl('proyecto')} target="_blank" rel="noreferrer">Agendar una conversación</a></Container></section></>}
