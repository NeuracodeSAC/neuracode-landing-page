import Button from '../components/Button'; import Container from '../components/Container'; import PageMeta from '../components/PageMeta'; import { caseCategories } from '../content'; import { whatsappUrl } from '../config'
export default function Casos(){return <><PageMeta path="/casos" title="Casos y evidencia — Neuracode" description="Experiencia y evidencia de Neuracode, publicada con contexto, atribución y permiso."/><section className="page-hero"><Container><p className="eyebrow">Casos y evidencia</p><h1>La evidencia necesita contexto.</h1><p>Este espacio publicará resultados, experiencia profesional, laboratorios e investigación con una clasificación visible y atribución correcta.</p></Container></section><section className="section"><Container><div className="filters" aria-label="Categorías">{caseCategories.map(c=><span key={c}>{c}</span>)}</div><div className="empty-state"><p className="eyebrow">En preparación</p><h2>No publicamos casos sin evidencia y permiso.</h2><p>Estamos documentando contexto, restricciones, enfoque, controles y resultados antes de convertir experiencia en una afirmación pública.</p><Button href={whatsappUrl('proyecto')} target="_blank" rel="noreferrer">Conversar sobre una necesidad similar</Button></div></Container></section></>}
